<%= simple_form_for([ @renting, message ], remote: true) do |f| %>
  <%= f.input :content, label:'Write your message' %>
  <%= f.submit "Leave a message", class: "btn yellow-btn text-center" %>
<% end %>
